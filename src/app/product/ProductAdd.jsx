import React from "react";
import ProductAddDialog from "@/module/product/components/ProductAddDialog";

const ProductAdd = (props) => {
  return <ProductAddDialog {...props} />;
};

export default ProductAdd;
