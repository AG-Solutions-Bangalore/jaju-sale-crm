import React from "react";
import ProductEditDialog from "@/module/product/components/ProductEditDialog";

const ProductEdit = (props) => {
  return <ProductEditDialog {...props} />;
};

export default ProductEdit;
