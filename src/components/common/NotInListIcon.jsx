import React from "react";
import { ListPlus } from "lucide-react";

export const NotInListIcon = ({ className = "h-4 w-4" }) => {
  return <ListPlus className={className} />;
};

export default NotInListIcon;
